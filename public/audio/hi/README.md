Recorded prompts for `hi`.

Drop MP3 files here named exactly as listed in `docs/RECORDING_SCRIPT.md`,
then add each key to the `hi` array in `src/i18n/recordings.js`.

Anything not listed there falls back to speech synthesis, so this folder can
be filled one clip at a time.
